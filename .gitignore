__pycache__/
*.pyc
.pytest_cache/

# Field data never belongs in this repo. Raw sweeps run to hundreds of MB, and
# both the captures and anything written from them carry live BSSIDs, SSIDs and
# client MAC addresses for a production network. Keep them on the capture
# machine; publish findings with the identifiers replaced instead.
pcaps/
hunt.db
